# DSH-Plugin-Profiler
DSH Plugin Profiler 是面向 DeepSeek Harness 的插件生命周期性能分析与可视化工具。它通过 DSH 现有扩展点采集插件加载、依赖等待、激活和重载过程中的耗时与状态数据，并在“设置 → 插件 → 性能”页面展示插件耗时排行和执行时间线。开发者可以判断性能问题来自插件自身初始化，还是上游依赖阻塞，从而快速定位拖慢系统的插件。

首版聚焦运行时分析，后续将扩展完整冷启动采样、关键路径计算、历史数据对比、性能回归检测和结果导出。
